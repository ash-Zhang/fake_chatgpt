# ChatGPT Clone

一个使用 Next.js 和 AI Builders API 构建的 ChatGPT 风格聊天界面。

## 功能特性

- 🎨 两列布局：左侧对话历史，右侧聊天窗口
- 💬 实时聊天：与 grok-4-fast 模型进行对话
- 📱 响应式设计：支持移动端和桌面端
- 🌙 深色模式：自动适配系统主题
- 📝 对话管理：创建、删除、切换对话
- ⚡ 流式响应：流畅的聊天体验

## 技术栈

- **Next.js 14** - React 框架
- **TypeScript** - 类型安全
- **Tailwind CSS** - 样式框架
- **AI Builders API** - 使用 grok-4-fast 模型

## 开始使用

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

确保 `.env.local` 文件包含：

```
AI_BUILDER_TOKEN=your_token_here
NEXT_PUBLIC_API_BASE_URL=https://space.ai-builders.com/backend
```

### 3. 运行开发服务器

```bash
npm run dev
```

在浏览器中打开 [http://localhost:3000](http://localhost:3000)

## 项目结构

```
├── app/
│   ├── api/chat/          # 聊天 API 路由
│   ├── globals.css        # 全局样式
│   ├── layout.tsx         # 根布局
│   └── page.tsx           # 主页面
├── components/
│   ├── Sidebar.tsx        # 侧边栏组件
│   ├── ChatWindow.tsx     # 聊天窗口组件
│   ├── MessageList.tsx    # 消息列表组件
│   └── InputArea.tsx      # 输入区域组件
└── types/
    └── index.ts           # TypeScript 类型定义
```

## 使用说明

1. **创建新对话**：点击左侧的"新对话"按钮
2. **发送消息**：在输入框中输入消息，按 Enter 发送
3. **切换对话**：点击左侧的对话列表项
4. **删除对话**：将鼠标悬停在对话上，点击删除图标

## 开发

```bash
# 开发模式
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm start

# 代码检查
npm run lint
```

## 许可证

MIT


